[STONKSMITH]
workspace = default
last_used_db =
audit_mode = False
reveal_chars_of_pwd = False
log_mode = False
host_info_colors = ("green", "red", "yellow", "cyan")

# Identifiers for the SnapTrade broker. Neither is a secret: the consumer key
# these pair with, and the user secret, live in the OS keyring. Create the user
# and store both secrets with scripts/snaptrade_register.py.
[SNAPTRADE]
clientid =
userid =
